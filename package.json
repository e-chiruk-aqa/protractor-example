{
  "name": "protractor-example",
  "version": "1.0.0",
  "dependencies": {
    "allure-commandline": "^2.13.0",
    "jasmine-allure-reporter": "^1.0.2",
    "protractor": "6.0.0"
  },
  "scripts": {
    "the-actual-test": "protractor conf.js",
    "test": "npm-run-all the-actual-test run-after-test-even-if-failed --continue-on-error",
    "run-after-test-even-if-failed": "allure generate allure-results --clean -o allure-report || true"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "chai-as-promised": "^7.1.1",
    "npm-run-all": "^4.1.5"
  }
}
