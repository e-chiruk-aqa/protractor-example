const TIMEOUTS = {
    MIN: 100,
    MEDIUM: 300,
    HIGH: 500,
    MAX: 1500,
};

const JASMINE_TIMEOUTS = {
    MEDIUM: 45000,
    MAX: 180000,
};

module.exports = Object.freeze({
    TIMEOUTS,
    JASMINE_TIMEOUTS,
});